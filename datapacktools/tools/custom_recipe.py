class CustomRecipe:

    def custom_recipe(self):
        self.window.clear()
        self.window.label("Custom crafting table recipe", font_size=18, pad=(10,10))
        self.pack_main_menu()

        self.window.label("Under Construction...", font_size=16, pad=(10,5))